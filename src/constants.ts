/** Directory name for cockpit configuration */
export const CONFIG_DIR = ".cockpit";

/** Workspace configuration file name */
export const WORKSPACES_FILE = "workspaces.ts";

/** Task configuration file name */
export const TASK_FILE = "cockpit.ts";

/** Cache directory within CONFIG_DIR */
export const CACHE_DIR = ".cache";

/** Default concurrency for parallel task execution */
export const DEFAULT_CONCURRENCY = 4;

/** Default task timeout in milliseconds (5 minutes) */
export const DEFAULT_TIMEOUT = 300000;
